{
  "name": "barbershop-backend",
  "version": "1.0.0",
  "description": "Modern barbershop fullstack application backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "node -e \"const fs=require('fs'); if(fs.existsSync('client/package.json')){ const {execSync}=require('child_process'); execSync('npm --prefix client install',{stdio:'inherit'}); execSync('npm --prefix client run build',{stdio:'inherit'}); } else { console.log('Skipping client build: client/package.json not found'); }\""
  },
  "keywords": [
    "barbershop",
    "express",
    "nodejs",
    "react",
    "fullstack"
  ],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express-rate-limit": "^6.10.0",
    "express-validator": "^7.0.1",
    "helmet": "^7.0.0",
    "googleapis": "^126.0.1",
    "nodemailer": "^6.9.4",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "react-toastify": "^9.1.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}